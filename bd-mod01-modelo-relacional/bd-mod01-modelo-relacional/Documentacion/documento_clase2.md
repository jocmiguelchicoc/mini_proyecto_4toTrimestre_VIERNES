volumen:
es una carpeta que se crea en el computador para guardar la base de datos


DEFAULT CURRENT_DATE:
TOMA LA FECHA ACTUAL Y LA HORA ACTUAL 


SERIAL
EL ID SE VA INCREMENTANDO A MEDIDA QUE VOY COLOCANDO NOMBRE DE UN ESTUDIANTES


INDICES  INDEX
se utiliza para mejorar las consultas del las columnas de las bases de datos



 
 codigo para ejecutar el docker en el puerto esto se coloca en la terminal con la ruta donde esta el docker-compose
 docker compose up -d



 este codigo se ejecuta en WSL 
 docker exec -it postgres16_bdcurso psql -U curso_user -d curso_bd

luego
\dn